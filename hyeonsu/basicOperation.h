#pragma once



char* Add(char* lhs, char* rhs);
char* Sub(char* lhs, char* rhs);
char* Mul(const char* lhs, const char* rhs);
char* Div(const char* lhs, const char* rhs, const char* remain);

void ReverseString(char* pszTarget);
